import DemoProfile from './profile'

const AccountFacebookProfile = ({data}) => {
  return (
    <div className="content-body">
      <DemoProfile data={data} />
    </div>
  )
}
export default AccountFacebookProfile
